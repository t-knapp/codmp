# Call of Duty 1.1 Multiplayer

## CoDExtended runtime image
The Dockerfile in [codextended-runtime](./codextended-runtime/) can be used to build a docker image 
with all dependencies for codextended lib.

